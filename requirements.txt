# Core analysis stack
numpy>=1.26
pandas>=2.1
scipy>=1.11
scikit-learn>=1.3
matplotlib>=3.8

# Web dashboard
Flask>=3.0

# Tests
pytest>=7.4

# --- Optional ---
# For the live LLM operator-assistant backend (otherwise the offline,
# knowledge-base backend is used automatically). Install with:
#   pip install anthropic
# and export ANTHROPIC_API_KEY=...
# anthropic>=0.40
